import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ExpenseGraph = ({ expenses }) => {
  return (
    <View>
      <Text>ExpenseGraph</Text>
    </View>
  );
};

export default ExpenseGraph;

const styles = StyleSheet.create({});
