{
  "rules": {
    "trips": {
      ".read": true,
      "$tripId": {
        "$userID": {
          ".validate": "auth.uid === $userID"
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
