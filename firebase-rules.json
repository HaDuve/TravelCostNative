{
  "rules": {
    "trips": {
      ".read": true,
      "$tripId": {
        "$userID": {
          ".validate": "auth.uid === $userID"
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}

{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}

{
  "rules": {
    ".read": "true",
    ".write": "true"
  }
}
{
  "rules": {
    ".read": "false",
    ".write": "false"
  }
}